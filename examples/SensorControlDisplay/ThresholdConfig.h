#ifndef ThresholdConfig_h
#define ThresholdConfig_h

// Default threshold for activating the control
#define DEFAULT_THRESHOLD 50

// Set to true for active-high (ie. device is on when sensor reads high), false for active-low behavior (ie. device is on when sensor reads low)
#define IS_ACTIVE_HIGH false

#endif